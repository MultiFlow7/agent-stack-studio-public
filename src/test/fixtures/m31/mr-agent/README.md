# MRAgent memory module

MRAgent stores conversation memory and resumable task state. It does not own the outer execution
loop. The static fallback is deliberately ambiguous so the structured remediation path must correct
its capabilities with evidence.
