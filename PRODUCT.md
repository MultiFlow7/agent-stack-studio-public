# Product

## Register

product

## Users

主要用户是需要搭建、调整和比较 Agent Stack 的科研人员及非技术用户。他们通常在自己的 Mac 上工作，希望复用现有 Harness 或开源项目，不希望先理解完整代码结构，也不希望为了更换一个模块而重建整个 Agent。

团队成员各自使用本地 Studio。需要共享、协作或分发任务时，将确认后的 Agent Version 发布到 Multica。

## Product Purpose

Agent Stack Studio 把一个 Agent 背后的 Harness、Workflow 和组件能力转化为可查看、可取舍、可替换的结构。用户可以识别一个项目已经覆盖哪些模块，决定重叠能力由哪个实现负责，锁定实验中的控制变量，并只调整目标变量。

成功意味着用户可以回答四个问题：当前 Agent 由什么组成，哪些部分可以调整，替换会影响什么，以及不同实验结果是否来自预期变量。

Studio 同时面向人和机器。任何能调用 Shell 的 Coding Agent、CI 或人工操作都可以通过厂商无关的 `studio` CLI 管理项目；GUI 可视化读取同一份 `.agent-stack`。Coding Agent 负责调研、下载和修改代码，Studio Core 负责静态描述、组合、验证、并发控制和记录。

## Brand Personality

克制、清晰、可信。界面应像一个可靠的研究工具，优先呈现结构、状态、来源和证据，不使用夸张表达，也不要求用户先掌握工程术语。

## Anti-references

- 不做以营销指标和装饰性卡片为中心的 SaaS 仪表盘。
- 不把流程图画布作为所有任务的默认入口。
- 不隐藏组件来源、版本、适配器或不可兼容状态。
- 不照搬 Multica 的代码、品牌资产或具体视觉样式。
- 不用复杂动画或视觉效果掩盖产品状态。

Multica 是交互结构参考：Agent 列表、详情页、分区设置和清楚的状态表达。参考的是信息组织方式，不是代码或视觉复制。

## Design Principles

1. Agent 是入口。用户先选择要管理的 Agent，再查看其 Stack、能力、实验和运行记录。
2. 先展示事实，再要求取舍。系统必须先标出组件覆盖、来源、依赖和冲突，才让用户选择替换方案。
3. 技术复杂度渐进呈现。默认使用能力名称和影响说明，高级用户可以展开版本、接口和运行时细节。
4. 实验配置必须可追溯。每次运行都绑定不可变的 Agent Version、变量、数据集和环境快照。
5. 本地优先，明确发布。编辑和运行默认只影响本机，发布到 Multica 必须是用户可见的主动操作。
6. 一个项目事实来源。可移植 Stack 定义只保存在 `.agent-stack`；SQLite 只保存本机索引与运行记录，GUI 和 CLI 不复制验证逻辑。
7. 发现不等于信任。远端 Provider 报告的元数据只用于筛选候选来源；下载、静态检查、导入和可信执行是四个分开的主动步骤。
8. 权限默认拒绝。Renderer 不获得浏览器权限、下载、导航或第三方嵌入能力；新增权限必须单独决策、最小开放并能在最终应用包中验证。
9. 不可变必须可验证。冻结 Version 在每次读取时重新计算快照哈希；不一致历史必须阻断或明确从最后有效备份恢复，不能继续显示为可信版本。
10. 密钥只在受信边界出现。原文只进入当前 Mac 的钥匙串和 Main/Runtime 受信调用，不进入 Renderer、项目文件、SQLite、备份或机器输出。
11. 运行信任必须精确。四种控制模式可以使用 Studio 内置、版本固定的本地 Profile；命名空间前缀、静态声明或用户更正都不能让第三方 Adapter 自动获得执行权限。

## Accessibility & Inclusion

界面遵守 WCAG 2.2 AA。正文和控件满足对比度要求；状态不能只依赖颜色；主要操作支持键盘；焦点清晰可见；表单和错误信息具有明确标签；遵循 macOS 的减少动态效果设置。专业术语需要提供自然语言说明。
